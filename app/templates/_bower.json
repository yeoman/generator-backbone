{
  "name": "<%= _.slugify(appname) %>",
  "version": "0.0.0",
  "dependencies": {<% if (compassBootstrap) { %>
    "sass-bootstrap": "~3.0.0",<% } %>
    "jquery": "~2.1.0",
    "underscore": "~1.6.0",
    "backbone": "~1.1.1",<% if (includeRequireJS) { %>
    "requirejs": "~2.1.11",
    "requirejs-text": "~2.0.10",<% } %>
    "modernizr": "~2.7.1"<% if (templateFramework === 'handlebars') { %>,
    "handlebars": "~1.3.0"<% } %>
  },
  "devDependencies": {}
}
